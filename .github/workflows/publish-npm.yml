name: Publish to NPM

on:
  push:
    tags:
      - 'v*'  # Dispara quando uma tag que começa com 'v' for criada
  workflow_dispatch:  # Permite execução manual

jobs:
  publish:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          registry-url: 'https://registry.npmjs.org'
          
      - name: Install dependencies
        run: npm ci
        
      - name: Run tests
        run: npm test
        
      - name: Build package
        run: npm run build
        
      - name: Publish to NPM
        run: npm publish --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
          
      - name: Create GitHub Release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ github.ref_name }}
          release_name: Release ${{ github.ref_name }}
          body: |
            ## 🚀 Nova versão do SDK Logger
            
            **Versão:** ${{ github.ref_name }}
            
            ### 📦 Instalação
            ```bash
            npm install @psouza.yuri/sdk-logger@${{ github.ref_name }}
            ```
            
            ### 🔧 Principais funcionalidades
            - ✅ Logs automáticos para todas as requisições HTTP
            - ✅ Integração nativa com SigNoz Cloud
            - ✅ Tracing com OpenTelemetry
            - ✅ Suporte completo ao NestJS
            - ✅ Configuração simples e intuitiva
            
            ### 📚 Documentação
            Consulte o [README.md](./README.md) para instruções detalhadas de uso.
          draft: false
          prerelease: false



